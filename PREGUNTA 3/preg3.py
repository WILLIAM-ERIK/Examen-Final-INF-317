from multiprocessing import Pool

def mensaje(texto)
    #pala = pala.lower()  # cambia a minusculas la palabra
    if str(texto) == str(texto)[-1] 
        return Verdad
    else
        return Falso

print(Pool().map(mensaje, ['123454321','edwin','oruro','Oruro']))